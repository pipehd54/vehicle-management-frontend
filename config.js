// Configuración de conexión con la API Backend (Railway o Local)
const CONFIG = {
    // URL de producción desplegada en Railway:
    API_BASE_URL: 'https://vehicle-management-backend-production-e9f4.up.railway.app'

    // Para pruebas de desarrollo local, cambia a:
    // API_BASE_URL: 'http://localhost:8000'
};
